# Copy to ".env" (gitignored) and fill in. docker-compose substitutes these into the
# freedomblaze service environment.

# OpenAI API key used for real-time Bitcoin news (Responses API + web search).
OPENAI_API_KEY=
